{
  "name": "code-jam-initializer",
  "version": "1.0.0",
  "description": "A command line tool to generate Google Code Jam starter files.",
  "scripts": {
    "build": "babel index.js -d dist",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "jam-init": "./dist/index.js"
  },
  "keywords": [
    "code",
    "jam",
    "practice"
  ],
  "author": "Sarah Edkins",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.2",
    "yargs": "^12.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-amex": "^1.1.0"
  }
}
